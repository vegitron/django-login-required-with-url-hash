#!/usr/bin/env python

from distutils.core import setup

setup(name='LoginRequired-HashURL',
      version='1.0',
      description='Allows you to require authentication on Django pages that use URL hashes for state.',
     )
